# Dead-Code-Audit — 2026-09-18

Branch: `feat/local-first-auth`. Baseline vor Änderungen: `npx tsc --noEmit` sauber, `npm test` 15 Suites / 76 Tests grün.
**Es wurde nichts gelöscht.** Diese Liste wartet auf Bestätigung; der Löschplan steht in Abschnitt 6.

Methodik: Import-Graph über `src/`, `App.tsx`, `__tests__/`, `bridge/api`, `webapp/`; jedes exportierte Symbol auf
Referenzen außerhalb der eigenen Datei geprüft; `tsc --noUnusedLocals --noUnusedParameters`; i18n-Schlüssel gegen
`t('…')`-Literale abgeglichen; Dependencies gegen tatsächliche Imports; Grep auf `anon`, `migrate`, `device_id`,
AsyncStorage-Keys; Heuristik auf auskommentierten Code.

---

## 1. Sicher tot — ganze Dateien

| # | Pfad | Zeilen | Begründung |
|---|---|---|---|
| 1 | `src/sync/deviceId.ts` | 31 | `getDeviceId`/`isDeviceIdFromEnv` werden nirgends importiert. Ersetzt durch `userId.ts` + `accountState.ts` (Commit `aa53950` „device_id → user_id"). Der Key `@notizapp_device_id` ist damit ebenfalls verwaist. |
| 2 | `src/components/GradientCard.tsx` | 41 | Kein Import im gesamten Projekt. Rest aus dem Navy/Gradient-Design vor der Amber-Migration. |
| 3 | `src/context/ThemeContext.tsx` | 17 | `useThemeMode` wird nirgends aufgerufen. `ThemeProvider` wrappt in `App.tsx` nur noch einen Kontext mit `isDark: true`, was seit dem hellen MD3-Theme auch inhaltlich falsch ist. Mit löschen: Wrapper in `App.tsx:132/154`. |
| 4 | `src/utils/categoryColors.ts` | 29 | `getCategoryColor` + alte Indigo/Mint-`PALETTE` ohne Referenz. `withAlpha` wird von 3 Settings-Screens genutzt, existiert aber byte-gleich in `src/theme/categoryAccents.ts` (dort mit Kommentar „Ersatz für withAlpha aus utils/categoryColors"). Importe umbiegen, Datei löschen. |
| 5 | `webapp/components/NoteOverlay.tsx` | 53 | Kein Import. Ersetzt durch `NoteViewModal` in `NoteModal.tsx` (Commit `b189154`). |
| 6 | `migration/` (gesamter Ordner) | 858 | Handoff-Paket der Amber-Migration vom 2026-05-02. Migration ist abgeschlossen: 4 der 5 Dateien sind byte-identisch mit `src/`, `NoteCard.tsx` ist in `src/` bereits weiterentwickelt. Nur Doku-Ballast. |
| 7 | `launch-reach-plan.md` (Repo-Root) | 148 | Ältere deutsche Fassung; `docs/launch-reach-plan.md` (258 Z., „living document", neuer) hat sie abgelöst. |
| 8 | `bridge/app.json` | 15 | Expo/EAS-Konfiguration (`projectId`, `updates.url`) im Vercel-Serverless-Projekt. Ohne Funktion, entstanden durch `eas`/`expo` im falschen Ordner (Commit `c5aba4e`). Dazu gehört das ignorierte `bridge/.expo/`. |
| 9 | `webapp/public/{file,globe,next,vercel,window}.svg` | — | create-next-app-Boilerplate, nirgends referenziert. |
| 10 | `webapp/README.md` | 36 | Unverändertes create-next-app-Boilerplate; die relevante Doku steht in `CLAUDE.md`. |

## 2. Sicher tot — Symbole in lebenden Dateien

| Datei | Symbol | Begründung |
|---|---|---|
| `src/theme/gradients.ts` | `Gradients`, `GradientName`, `getCategoryGradient`, `Shadows.soft` | 0 Referenzen. Laut Datei-Kommentar bewusst als „Kompat-Export, damit Imports nicht brechen" — es importiert sie aber niemand mehr. `Radii`, `Shadows.softWarm`, `Insets` bleiben (10+ Nutzer); `Shadows.glow` 1× in `VoiceCaptureSheet`. |
| `src/sync/supabaseClient.ts` | `getExistingUserId` | 0 Referenzen; Aufgabe übernimmt `resolveAccountState()`. |
| `src/i18n/index.ts` | `SUPPORTED_LOCALES` | 0 Referenzen. |
| `src/models/Note.ts` | `WEEKDAY_LABELS` | 0 Referenzen; hart kodiertes Deutsch, ersetzt durch i18n-Key `editor.weekdays`. |
| `src/utils/haptics.ts` | `heavy`, `warning` | 0 Referenzen. |
| `src/utils/notifications.ts` | `scheduleTestRecurring` | 0 Referenzen (Dev-Helfer). |
| `webapp/types/index.ts` | `GraphNode`, `GraphLink` | Kommentar „kept for backward compatibility with the API route" — die Route nutzt sie nicht. |
| `src/screens/onboarding/index.tsx` | `activeIndex` State (Z. 44) | Gesetzt, nie gelesen; `activeIndexRef` erledigt die Arbeit. Löst nur unnötige Re-Renders aus. |
| `App.tsx` | `export` vor `navigationRef` | Nur intern genutzt; Export ist überflüssig (kein Löschkandidat, nur `export` entfernen). |

**Ungenutzte Imports/Variablen** (Ausgabe von `tsc --noUnusedLocals`), alle gefahrlos entfernbar:

```
src/components/VoiceCaptureSheet.tsx:57   theme
src/screens/ArchiveScreen.tsx:12          Shadows
src/screens/HomeScreen.tsx:13             Radii
src/screens/onboarding/index.tsx:5-6      NativeScrollEvent, NativeSyntheticEvent
src/screens/onboarding/index.tsx:12       Tokens
src/screens/onboarding/SlideValue1.tsx:8  CREAM
src/screens/onboarding/SlideValue2.tsx:9  CREAM
src/screens/SettingsAboScreen.tsx:10      AppLocale
src/screens/SettingsKontoScreen.tsx:23    Type
src/screens/SettingsWebAppScreen.tsx:6    Type, Fonts (ganze Import-Zeile)
src/screens/SettingsWebAppScreen.tsx:7    Insets
src/screens/ThreadDetailScreen.tsx:10     Type
src/screens/ThreadsScreen.tsx:54-55       index, theme
```

**Ungenutzte i18n-Schlüssel** (in `de.ts` und `en.ts`, je 10): `common.save`, `common.saving`, `common.remove`,
`common.error`, `common.errorPrefix`, `common.note`, `common.notes`, `common.category`, `common.categories`,
`editor.reminderOnDate`. Alle weiteren Treffer des Skripts (`editor.weekdays.*`, `*.one/_other`-Plurale,
`threads.serverError.*`) sind Array-/Plural-/Template-Zugriffe und **in Nutzung**.

## 3. Unsicher — manuell prüfen

| Was | Befund | Empfehlung |
|---|---|---|
| `package.json`: `expo-av` | 0 Imports, kein Config-Plugin. Sprachaufnahme läuft über `expo-speech-recognition`. | Sehr wahrscheinlich tot. `npm uninstall`, dann Dev-Build starten. |
| `package.json`: `expo-linear-gradient` | 0 Imports (CLAUDE.md verbietet es sogar); der einzige `LinearGradient` ist der aus `react-native-svg` in `VelmLogo`. | Tot. Entfernen. |
| `package.json`: `react-native-vector-icons` + `@types/react-native-vector-icons` | 0 Imports. react-native-paper 5.15 nutzt intern `@react-native-vector-icons/material-design-icons`, nicht mehr das alte Paket. | Wahrscheinlich tot, aber Metro-Aliasing möglich → nach `uninstall` einen nativen Build prüfen, ob Paper-Icons rendern. |
| `package.json`: `expo-updates`, `react-native-screens`, `react-test-renderer` | 0 direkte Imports, aber: `app.json` hat `updates`/`runtimeVersion`; `react-native-screens` ist Peer von React Navigation; `react-test-renderer` Peer der Testing Library. | **Behalten.** |
| `bridge/package.json`: `@supabase/supabase-js`, `expo-updates` | 0 Imports in `bridge/api` und `bridge/worker` (alles läuft über `fetch`). | Tot; entfernen zusammen mit `bridge/app.json`. |
| `bridge/worker/brainstorm-worker.mjs`, `similarity-worker.mjs`, `similarity-prompt.md` | Nicht von der API referenziert; laut Kopfkommentar „nur noch manuelles Lesen/Schreiben". Nutzen `BRIDGE_USER_ID`/`DEVICE_ID` aus dem alten Modell. | Nur du weißt, ob du sie noch per Hand aufrufst. Wenn nicht: löschen (468 Z. + Logs). |
| `src/sync/legacyAnon.ts` + `isLegacyAnonymous()` in `accountState.ts` + Aufruf `NotesContext.tsx:356` + zugehörige Tests | Bewusst gehaltene Übergangs-Altlast (CLAUDE.md): meldet beim Start alte anonyme Sessions ab. | Behalten, bis alle Geräte einmal die neue Version gestartet haben; danach als eigener Commit entfernen. **Stale Kommentar:** verweist auf `scripts/cleanup-anon-users.sql`, das nicht existiert. |
| `supabase-schema.sql` + `supabase-migration-2026-09-17.sql`: RPC `migrate_user` | Laut CLAUDE.md funktionslos (Altlast des anonymen Modells). | `drop function public.migrate_user` manuell in Supabase; SQL-Dateien bereinigen. Kommentar „auch anonym" beim Profile-Trigger anpassen. |
| `bridge/api/_lib/supabaseAdmin.ts`: `VerifiedUser.is_anonymous` + 403 in `bookmarklet-token.ts` | Noch aktiver Schutz; neue anonyme User entstehen zwar nicht mehr, alte könnten aber noch Tokens haben. | Behalten, solange `legacyAnon.ts` lebt. |
| `src/screens/SettingsSynchronisationScreen.tsx` | Erreichbar über Einstellungen, zeigt nur Sync-Status + UID zum Kopieren. Überschneidet sich mit `SettingsKontoScreen`. | Produktentscheidung, kein toter Code. |
| `finish-plan.md` (660 Z.) | Planungsdokument vom Mai; `docs/launch-reach-plan.md` verweist nicht mehr darauf. | Doku — nach `docs/` verschieben oder löschen. |
| `store/privacy/index.html` | Duplikat von `webapp/app/privacy/page.tsx` (App verlinkt `velmnote.com/privacy`, also die Webapp-Route). | Wahrscheinlich tot. **Wichtiger:** beide Fassungen behaupten noch „Velm erstellt beim ersten Start automatisch ein anonymes Konto" — inhaltlich falsch seit Lokal-first. Text aktualisieren. |
| `docs/audit/manual-tests.md`, `docs/audit/report.md` | Erwähnen anonymen User / `migrateAndDeleteAnonUser` als historischen Zustand. | Historische Doku, behalten. |
| Lokale Artefakte (ignoriert): `dist/` (36 MB), `.vercel/` + `.env.local` im App-Root (zeigen auf Vercel-Projekt „bridge", `vercel` im falschen Ordner ausgeführt), `bridge/.expo/`, `bridge/worker/logs/` | Nicht im Git. | Lokal löschen, kein Commit nötig. |

## 4. Auskommentierter Code

Keiner gefunden. Die Heuristik (`// import`, `// const`, `// <Komponente`, `/* … */`-Blöcke) über `src/`, `App.tsx`,
`bridge/api`, `webapp/` lieferte nur erklärende Kommentare. Veraltete Kommentare, die bei der Bereinigung mitgehen sollten:
`gradients.ts` Kopf („Drop-in Ersatz für die alte gradients.ts", „Legacy-Map"), `categoryAccents.ts:47-53`
(„Backward-compat-Wrapper", „Ersatz für withAlpha"), `legacyAnon.ts:14-16` (nicht existierendes SQL-Skript),
`webapp/types/index.ts` („Legacy node/link shape").

## 5. Reste des alten Anmeldesystems und AsyncStorage-Keys

- `migrateAndDeleteAnonUser`: **im Code vollständig entfernt**, nur noch in `docs/audit/report.md` als Historie.
- `signInAnonymously` / `/api/migrate-user`: entfernt. Übrig: `migrate_user`-RPC im SQL (siehe Abschnitt 3).
- Bewusst verbliebene Altlast: `legacyAnon.ts`-Pfad (Abschnitt 3).
- `device_id`-Modell: `deviceId.ts` (Abschnitt 1). Die Variablen `deviceIdRef`/`deviceId` in `NotesContext`/`ThoughtsContext`
  enthalten inzwischen die User-UID — der Name ist irreführend, aber nur Kosmetik (Umbenennung in `uidRef` empfohlen).
- AsyncStorage-Keys (14 gefunden): alle bis auf `@notizapp_device_id` in Nutzung. `@notizapp/haptics_enabled` weicht mit Slash
  von der `@notizapp_`-Konvention ab. `deleteAllData()` räumt 6 Notiz-Keys; Konto-/Onboarding-/Sprach-Keys bleiben absichtlich stehen.
  `@notizapp_device_id` liegt auf Altgeräten weiter im Storage — harmlos; optional in `deleteAllData` mit aufnehmen.

## 6. Doppelte Logik (Refactoring, kein Löschen)

1. **Bridge-Helfer doppelt:** `bridge/api/synthesize.ts` definiert eigene `verifyToken`, `setCors`, `sbHeaders` (+ `sbGet/Post/Patch/Delete`)
   parallel zu `bridge/api/_lib/supabaseAdmin.ts`, das `note.ts`, `delete-user.ts`, `bookmarklet-token.ts` nutzen. `verifyToken` hat
   abweichende Rückgabe (`string` statt `VerifiedUser`). `Tier`/`ProfileRow` sind dreifach definiert (synthesize, _lib, App).
2. **`withAlpha` + Hash-Funktion** 2× (`utils/categoryColors.ts`, `theme/categoryAccents.ts`) — erledigt sich mit Abschnitt 1 Nr. 4.
3. **Locale-Mapping** `locale === 'en' ? 'en-US' : 'de-DE'` 9× in 8 Dateien (`NoteCard`, `ArchiveScreen` 2×, `EditorScreen`, `HomeScreen`,
   `NoteDetailScreen`, `ThreadDetailScreen`, `VoiceCaptureSheet`, `limitFormat.ts` hat bereits `intl()`). Ein `toIntlLocale()` in `src/i18n/index.ts`.
4. **Settings-Zeilen-Komponenten** 4× kopiert mit identischen Styles (`row`, `rowIcon`, `divider`): `NavRow` (SettingsScreen),
   `ActionRow` (Benachrichtigungen), `SettingRow` (Darstellung), `InfoRow` (Synchronisation). Eine `SettingsRow` mit `trailing`/`onPress`.
5. **Toast-State** (`toast`, `toastKey`, `showToast`) identisch in `SettingsKontoScreen` und `SettingsDatenschutzScreen` → `useToast()`.
6. **`Unsubscribe`-Typ** 2× (`remoteNotes.ts`, `remoteThoughts.ts`). Kosmetik.
7. **Rate-Limit-Regeln** in App (`computeNextAllowedAt`) und Bridge (`checkRateLimit`) — beabsichtigt (Server entscheidet), nur Hinweis.

## 7. Löschplan nach Freigabe — ein Commit pro Schritt

Nach jedem Schritt: `npx tsc --noEmit` + `npm test` (Erwartung: 15 Suites / 76 Tests grün).

1. `chore: ungenutzte Imports und Variablen entfernen` — die 16 tsc-Treffer + `activeIndex` + `export` an `navigationRef`.
2. `refactor(theme): categoryColors.ts durch categoryAccents.withAlpha ersetzen` — 3 Importe umbiegen, Datei löschen.
3. `chore: deviceId.ts, GradientCard.tsx, ThemeContext.tsx entfernen` — inkl. `ThemeProvider`-Wrapper in `App.tsx`.
4. `chore: tote Exporte entfernen` — `Gradients`, `GradientName`, `getCategoryGradient`, `Shadows.soft`, `getExistingUserId`,
   `SUPPORTED_LOCALES`, `WEEKDAY_LABELS`, `heavy`, `warning`, `scheduleTestRecurring`; veraltete Kommentare in `gradients.ts`/`categoryAccents.ts`.
5. `chore(i18n): 10 ungenutzte Schlüssel aus de/en entfernen`.
6. `chore(deps): expo-av, expo-linear-gradient, react-native-vector-icons entfernen` — `npm uninstall`, danach Dev-Build prüfen (Paper-Icons).
7. `chore(repo): migration/ und veralteten launch-reach-plan.md entfernen` (+ optional `finish-plan.md` nach `docs/`).
8. `chore(bridge): app.json und ungenutzte Dependencies entfernen`, danach separat `refactor(bridge): synthesize.ts auf _lib/supabaseAdmin umstellen`.
9. `chore(webapp): NoteOverlay, Boilerplate-SVGs, README, GraphNode/GraphLink entfernen`.
10. Optional nach Sunset: `chore(auth): legacyAnon-Pfad entfernen` + `migrate_user` droppen + Datenschutztext korrigieren.

Danach: `CLAUDE.md` (Zeile 27 nennt `legacyAnon`, Komponentenliste in Zeile 22 nennt `GradientCard`) anpassen.

## 8. Zusammenfassung

- **Sicher tot:** 10 Dateien/Ordner (≈ 1.270 Zeilen, davon 858 im `migration/`-Ordner und 148 Doku), 12 tote Exporte,
  16 ungenutzte Imports, 20 i18n-Einträge.
- **Unsicher:** 4 Dependencies in der App, 2 in der Bridge, 3 Worker-Skripte, der bewusst gehaltene Anon-Sunset-Pfad.
- **Muster:** Drei Refactorings haben jeweils „Kompat-Schichten" hinterlassen, die nie abgebaut wurden:
  (a) die Amber-Migration (Mai) — `gradients.ts`-Kompat-Exporte, `GradientCard`, `categoryColors.ts`, `ThemeContext` mit `isDark`,
  der komplette `migration/`-Ordner; (b) der Wechsel device_id → user_id (April) — `deviceId.ts`, die weiter `deviceId` genannten
  Variablen; (c) der Umbau anonym → Lokal-first (September) — sauber aufgeräumt im Code, Reste nur noch in SQL-RPC, Datenschutztext
  und Doku. Dazu Boilerplate-Rauschen in `webapp/` und ein versehentlich mit Expo initialisierter `bridge/`-Ordner.
- Auskommentierter Code: keiner.
